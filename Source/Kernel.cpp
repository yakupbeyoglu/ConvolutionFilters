#include "Kernel.h"



/*    Kernel(const std::initializer_list<T_> &list);
    Kernel(const std::initializer_list<std::initializer_list<T_>> &list);
    // copy constructor
    Kernel(const Kernel &ckernel);
    
    Kernel &operator = (const Kernel &other);
    
    void SetWidth(int width);
    void SetHeight(int height);
    void SetSize(Gorgon::Geometry::Size size);
    
    int GetWidth()const;
    int GetHeight()const;
    Gorgon::Geometry::Size GetSize()const;
    
    
    
    
    
private :
    Gorgon::Geometry::Size size;
    std::vector<T_> kernel; */
